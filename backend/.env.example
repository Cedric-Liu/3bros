# 应用配置
DEBUG=false

# CORS配置（前端地址）
CORS_ORIGINS=["http://localhost:5173","https://3bros.vercel.app"]

# 数据库路径（可选，默认使用项目根目录/data/3bros.db）
# DATABASE_PATH=/path/to/3bros.db

# Server酱配置（可选，也可以在设置页面配置）
# SERVERCHAN_KEY=your_send_key_here
